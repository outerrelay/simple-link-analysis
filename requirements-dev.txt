-r requirements.txt
pytest>=8.3,<9.0
pytest-asyncio>=0.25,<2.0
# Ruff's formatter output can change between minor versions, which would make
# `ruff format --check` fail on code that was fine. Held to one minor line.
ruff>=0.8,<0.9
